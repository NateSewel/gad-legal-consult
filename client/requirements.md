## Packages
(none needed)

## Notes
Uses existing shadcn/ui components already present in client/src/components/ui
No auth flows; all requests use credentials: "include" per template
SEO tags are applied client-side via document.title + meta updates (no SSR)
Smooth-scroll uses built-in browser behavior + small helper
